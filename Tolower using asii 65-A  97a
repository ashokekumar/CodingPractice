class Solution {
public:
    string toLowerCase(string s) {
        
    for(int i=0;i<s.size();i++){

        if(s[i]>=65 && s[i]<=90){  //A== 65, Z==90
          s[i] = s[i]+32;  //a=97
        }
    }
        return s;
    }
};
